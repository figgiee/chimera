---
phase: 04-sessions-health-knowledge
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - chimera-chat.js
  - web/src/lib/chat/types.ts
  - web/src/lib/components/SessionSidebar.svelte
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Each session row in the sidebar shows a last-message preview below the title"
  artifacts:
    - path: "chimera-chat.js"
      provides: "lastMessagePreview field in GET /api/sessions response"
      contains: "lastMessagePreview"
    - path: "web/src/lib/chat/types.ts"
      provides: "lastMessagePreview on SessionInfo interface"
      contains: "lastMessagePreview"
    - path: "web/src/lib/components/SessionSidebar.svelte"
      provides: "Rendered preview text in session row"
      contains: "lastMessagePreview"
  key_links:
    - from: "chimera-chat.js"
      to: "web/src/lib/chat/types.ts"
      via: "JSON response shape matches SessionInfo interface"
      pattern: "lastMessagePreview"
    - from: "web/src/lib/components/SessionSidebar.svelte"
      to: "session.lastMessagePreview"
      via: "Renders preview string from typed SessionInfo"
      pattern: "session\\.lastMessagePreview"
---

<objective>
Close the one verification gap from Phase 4: add last-message preview to the session sidebar rows.

Purpose: The success criterion requires "last-message previews" in session rows. Currently, rows show title + relative timestamp but no preview of the most recent message. Three surgical edits close this gap.
Output: Backend computes preview, type carries it, sidebar renders it.
</objective>

<execution_context>
@C:\Users\sandv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sandv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sessions-health-knowledge/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lastMessagePreview to backend and type</name>
  <files>chimera-chat.js, web/src/lib/chat/types.ts</files>
  <action>
Two changes:

1. **chimera-chat.js** (lines 222-232, GET /api/sessions handler): After computing `title` from the first user message, compute `lastMessagePreview` from the LAST log entry. Find the last entry in `entry.logs` array (regardless of type — could be user_message or assistant text). Extract its `.text` property, truncate to 80 characters, append ellipsis if truncated. If no logs exist, use empty string. Add `lastMessagePreview` to the object pushed into `sessionList`.

Specifically, after line 227 (`const messageCount = ...`), add:
```js
const lastLog = entry.logs.length ? entry.logs[entry.logs.length - 1] : null;
const lastMessagePreview = lastLog?.text ? (lastLog.text.length > 80 ? lastLog.text.slice(0, 80) + '...' : lastLog.text) : '';
```
Then include `lastMessagePreview` in the pushed object on line 228.

2. **web/src/lib/chat/types.ts** (SessionInfo interface, line 185-191): Add `lastMessagePreview: string;` field after `messageCount`.
  </action>
  <verify>
Run: `node -e "const src = require('fs').readFileSync('chimera-chat.js','utf8'); console.log(src.includes('lastMessagePreview'));"` — should print true.
Run: `cd web && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -20` — no type errors related to SessionInfo.
  </verify>
  <done>GET /api/sessions response includes lastMessagePreview for each session. SessionInfo type includes the field. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Render last-message preview in session sidebar rows</name>
  <files>web/src/lib/components/SessionSidebar.svelte</files>
  <action>
In SessionSidebar.svelte, find the session row button (around line 215-223) that currently renders:
- `session.title` in a truncate span
- `relativeTime(session.lastActive)` in a smaller span

Add a third line between the title and the timestamp to show the preview. Insert a new span:
```svelte
{#if session.lastMessagePreview}
  <span class="truncate text-[11px] text-muted-foreground/70 leading-tight">{session.lastMessagePreview}</span>
{/if}
```

Place this after the title span (line 221) and before the timestamp span (line 222). The preview text should be slightly smaller than the title but larger than the timestamp, using muted foreground at reduced opacity for visual hierarchy: title (bold, foreground) > preview (muted) > timestamp (smallest, muted).
  </action>
  <verify>
Run: `cd web && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -20` — no errors.
Run: `cd web && npm run build 2>&1 | tail -10` — build succeeds.
  </verify>
  <done>Session rows display three lines: title, last-message preview (if available), and relative timestamp. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `svelte-check` passes with no errors
2. `npm run build` in web/ succeeds
3. Grep confirms lastMessagePreview appears in all three files: chimera-chat.js, types.ts, SessionSidebar.svelte
</verification>

<success_criteria>
- GET /api/sessions response objects include a lastMessagePreview string (up to 80 chars + ellipsis)
- SessionInfo TypeScript interface includes lastMessagePreview: string
- Session sidebar rows render the preview as a second line between title and timestamp
- All type checks and builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-sessions-health-knowledge/04-04-SUMMARY.md`
</output>
