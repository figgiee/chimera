services:
  # PostgreSQL with pgvector for vector storage
  postgres:
    image: pgvector/pgvector:pg15
    container_name: chimera-postgres
    environment:
      POSTGRES_DB: nexus_rag
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: ${DB_PASSWORD:-nexus_secure_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexus -d nexus_rag"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rag-network

  # Text Embeddings Inference for vectorization
  tei:
    image: ghcr.io/huggingface/text-embeddings-inference:cpu-latest
    container_name: chimera-tei
    environment:
      MODEL_ID: sentence-transformers/all-MiniLM-L6-v2
      DTYPE: float32
    volumes:
      - tei_cache:/root/.cache
    ports:
      - "8001:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rag-network

  # SearXNG - Privacy-respecting meta search engine
  searxng:
    image: searxng/searxng:latest
    container_name: chimera-searxng
    volumes:
      - ./searxng-settings.yml:/etc/searxng/settings.yml:ro
      - searxng_data:/etc/searxng
    ports:
      - "8888:8080"
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz')\""]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rag-network

  # Custom RAG Orchestrator
  rag-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chimera-rag-server
    depends_on:
      postgres:
        condition: service_healthy
      tei:
        condition: service_healthy
      searxng:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: ${DB_PASSWORD:-nexus_secure_password}
      POSTGRES_DB: nexus_rag
      POSTGRES_PORT: 5432
      TEI_URL: http://tei:80
      SEARXNG_URL: http://searxng:8080
      LM_STUDIO_URL: http://host.docker.internal:1234
      LOG_LEVEL: INFO
    volumes:
      - rag_data:/app/data
      - ./documents:/app/documents
    ports:
      - "8080:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rag-network

networks:
  rag-network:
    driver: bridge

volumes:
  postgres_data:
  tei_cache:
  rag_data:
  searxng_data:
